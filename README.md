# Webhook Verification Prototype

A lightweight Flask API that verifies incoming HTTP POST webhooks using HMAC-SHA256 signature matching.

## How to Run

1. **Install dependencies:**
   ```cmd
   python -m pip install flask
   ```
